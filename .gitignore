node_modules/
.env
.env.local
.next/
frontend/next-build/
dist/
coverage/
.DS_Store
uploads/
*.log
*.pid
*.zip
pnpm-lock.yaml
pnpm-workspace.yaml
Claude outputs/
schema_dump.sql
backend/migration/
.env.production
.env*.local
hostinger.env

# Local-only: deployment secrets and the local SQLite DB / uploads
.env.hostinger
.localdata/

# Fallback database location (used only when the persistent path is unusable)
data/
